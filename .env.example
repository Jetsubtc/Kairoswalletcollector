# PostgreSQL Database Configuration
# For local development with PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hyperwalletcollector

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# For production deployment with a service like Supabase, Render, or ElephantSQL
# DATABASE_URL=postgresql://user:password@host:port/database

# Example for Supabase (replace with your actual credentials)
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Example for Render (replace with your actual credentials)
# DATABASE_URL=postgresql://user:password@host:port/database